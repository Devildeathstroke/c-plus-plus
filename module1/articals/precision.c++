//floor,ceil
#include <bits/stdc++.h>
using namespace std;

int main() {
    double x=1.411,a=-1.411;
    // cout<<floor(x) <<floor(a) <<endl; //1 and -2
    // cout<<ceil(x) <<ceil(a) <<endl; //2 and -1
    // cout<<trunc(x) <<trunc(a) <<endl; //1 and -1
    //  cout<<round(x) <<round(a) <<endl; //1 and -1
     cout<<fixed <<setprecision(0)<<x<<" "<<a <<endl; //1 and -1
    return 0;
}